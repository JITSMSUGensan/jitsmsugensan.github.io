import PageScreen from "../../components/PageScreen";

export default function Landing() { 
    return (
        <>
            <PageScreen>
                <></>
                {
                //Landing
                // Carousel
                    // Slide (title, description, image, link)
                // News
                    // NewsItem (title, description, link)
                }
            </PageScreen>
            <PageScreen>
                <></>
                {
                //Landing
                // Carousel
                    // Slide (title, description, image, link)
                // News
                    // NewsItem (title, description, link)
                }
            </PageScreen>
        </>
    )
}